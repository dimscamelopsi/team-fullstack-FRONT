import { SessionStorageStrategy } from './session-storage-strategy';

describe('SessionStorageStrategy', () => {
  it('should create an instance', () => {
    expect(new SessionStorageStrategy()).toBeTruthy();
  });
});
